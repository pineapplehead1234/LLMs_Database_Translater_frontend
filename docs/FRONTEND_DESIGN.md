翻译应用前端设计文档

📋 项目概述

- 项目名称: PC端翻译应用
- 开发周期: 1个月
- 技术栈: Vue3 + TypeScript + Element Plus + Electron + Pinia
- 参考设计: Obsidian + VS Code
- 后端: 支持高并发，无需登录
- 支持文档格式: markdown, PDF, docx

---

🎨 UI 布局设计

最终布局

┌─────────────────────────────────────────────────────────────┐
│ ☰ 翻译助手 |[🌓] [⚙️]      │ 顶部工具栏
├────────────┬────────────────────────────┬───────────────────┤
│ [文件][RAG] │  📄doc1 ✓  📄doc2 ✓ 📄doc3  │                    │
│ ┌────────┐ │ ─────────────────────────── │   译文显示区      │
│ │📤上传文档││                            │                   │
│ ├────────┤ │    原文展示区                │ ┌───────────────┐ │
│ │拖拽到这里│ │                            │ │              │ │
│ │或点击上传││ ┌──────────────────────────┐│ │   翻译结果    │ │
│ └────────┘ │ │                          ││ │  (自动跟随)   │ │
│ 📁 文件树  │ │  [可编辑TextArea]         ││ │              │ │
│            │ │                           ││ │  Hello World  │ │
│ + 新建    │ │  Hello World             ││ │  → 你好世界   │ │
│            │ │  Test Content             ││ │              │ │
│ 📂 根目录  │ │                           ││ │              │ │
│  📄 doc1✓ │ └──────────────────────────┘│ └───────────────┘ │
│  📄 doc2✓ │    [→ 翻译 →] 按钮           │                   │
│           │                             │                   │
│ 📂 工作   │                             │                   │
│ 📂 学习   │                             │                   │
└────────────┴────────────────────────────┴───────────────────┘
                                                      ↓
                                            [进度浮窗 右下角]

布局说明

- 顶部: 工具栏（应用标题、导航按钮、语言选择、主题切换、设置）
- 左侧: 文件树（上传按钮、文件夹管理、文件列表）
- 中间上方: 标签页切换区（原文标签）
- 中间下方: 原文编辑区（可编辑）
- 右侧上方: 标签页切换区（译文标签）
- 右侧下方: 译文显示区（只读）
- 右下角: 上传/翻译进度通知浮窗

视图切换：
- 点击"文件管理"按钮：显示文件树和翻译视图（默认视图）
- 点击"知识库"按钮：切换到知识库管理视图（显示CSV管理和手动术语管理）

---

📤 文件上传功能

上传入口

1. 文件树上方: 📤 上传文件 
2. 每个文件夹: + 按钮（直接上传到该文件夹）
上传区域详细设计

┌─────────────────────────────────────┐
│      拖拽文件到这里或点击上传         │
│                                     │
│ 已选择 3 个文件:                   │
│ 📄 doc1.txt      [状态]           │
│ 📄 doc2.pdf      [状态]           │
│ 📄 doc3.txt      [状态]           │
│                                  │ 
│ 模式                             ▼│ 
│ 保存到: 📁 根目录               ▼ │
└─────────────────────────────────────┘

进度浮窗（右下角）

- 翻译进度: 后端返回结果就 +1（如：翻译进度 1/5）
- 显示格式: 已完成/总数 + 百分比进度条 + 当前文件名
┌─────────────────────────────┐
│  📤 正在处理            [×] │
├─────────────────────────────┤
│                             │
│  翻译进度: 1/5 (20%)       │
│  ████░░░░░░░░░░            │
│  doc1.txt 已完成           │
└─────────────────────────────┘


---

📁 文件树功能

层级限制

- 建议: 限制为 2-3 层
功能列表

3. 创建文件夹: 通过 + 按钮
4. 重命名: 右键菜单
5. 删除: 右键菜单
6. 拖拽移动: 支持拖拽文件到不同文件夹
7. 文件夹上传: 每个文件夹有 + 按钮
右键菜单

- 新建文件夹
- 重命名
- 删除
- 移动到...


---

🌐 翻译功能

翻译流程

用户点击文件树节点
  ↓
显示原文在左侧编辑区（标签页）
  ↓
用户点击【→ 翻译 →】按钮
  ↓
右下角显示"翻译进度: 1/5"
  ↓
后端并发处理，返回结果
  ↓
显示译文在右侧（标签页）
  ↓
自动保存翻译记录

并发策略

- 前端：同时发送多个翻译请求
- 进度：收到一个结果就更新一次进度
- 不限制并发数（后端支持高并发）
- 显示格式：已完成/总数

---

🔄 自动打开文件

核心特性

- 翻译完成后自动切换到最后完成的文件
- 保留最近打开的 N 个文件（如5个）
- 以堆叠标签页形式切换
标签页设计

原文区域标签页:
📄 doc1 ✓ | 📄 doc2 ✓ | 📄 doc3

自动打开触发流程

第1个文件翻译完成
  ↓
自动切换并打开该文件
  ↓
显示原文和译文
  ↓
右下角通知："doc1.txt 翻译完成"
  ↓
添加标签页到堆叠
  ↓
第2个文件翻译完成
  ↓
自动切换到 doc2（doc1 在标签页堆叠中）
  ↓
用户可以在标签页切换回 doc1


---

🌓 主题切换

实现方式

- 使用 @vueuse/core 的 useDark
- 顶部工具栏 🌓 下拉选择
- 选项：跟随系统、浅色、深色
Store 设计

// stores/settingStore.ts
export const useSettingStore = defineStore('setting', () => {
  const colorMode = useDark()
  // 主题切换逻辑
})


---

💾 本地缓存

存储方案

- 使用 localStorage + JSON
存储内容

9. 翻译记录（原文、译文、时间）
10. 文件夹结构
11. 用户设置（主题）
12. 最近打开的 N 个文件
数据结构

// 翻译记录
interface TranslationRecord {
  id: string
  title: string
  originalText: string
  translatedText: string
  sourceLang: string
  targetLang: string
  folderPath: string
  createdAt: string
  updatedAt: string
}

// 文件夹
interface Folder {
  id: string
  name: string
  parentId: string | null
  translations: string[]
  createdAt: string
}

// 文件树节点
interface TreeNode {
  id: string
  name: string
  type: 'folder' | 'file'
  children?: TreeNode[]
  parentId: string | null
  translationId?: string
}


---

📚 知识库管理

功能概述

- 支持CSV文件导入自定义术语库
- 支持手动添加/编辑/删除术语
- 翻译时自动使用知识库提升翻译质量
- 所有数据存储在本地（localStorage）

UI布局

┌─────────────────────────────────────────────────────────────┐
│                    [知识库管理]                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ [上半区：CSV知识库]                                     │ │
│  │                                                         │ │
│  │  📤 [上传CSV文件]                                       │ │
│  │                                                         │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ glossary.csv    (45条)    [删除] [导出]        │ │ │
│  │  │ medical.csv     (30条)    [删除] [导出]        │ │ │
│  │  │ ai_terms.csv    (20条)    [删除] [导出]        │ │ │
│  │  │     (可垂直滚动)                                 │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ [下半区：我的术语]                     [添加] [批量删除] │ │
│  │                                                         │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │ 中文          │ 英文              │ 操作          │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │ 神经网络      │ Neural Network    │ [编辑] [删除]  │ │ │
│  │  │ 深度学习      │ Deep Learning     │ [编辑] [删除]  │ │ │
│  │  │ 人工智能      │ AI                │ [编辑] [删除]  │ │ │
│  │  │ ...           │ ...               │ ...           │ │ │
│  │  │      (可横向 + 垂直滚动)                         │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘

CSV格式规范

固定格式（必须严格遵循）：
```csv
chinese,english
神经网络,Neural Network
深度学习,Deep Learning
人工智能,Artificial Intelligence
```

功能列表

1. CSV文件上传
   - 上传按钮（拖拽或点击）
   - 文件解析和验证
   - 显示条目数量

2. CSV文件管理
   - 文件列表（文件名 + 条目数）
   - 删除文件
   - 导出文件
   - 文件信息展示

3. 手动术语管理
   - 添加条目（弹窗表单）
   - 编辑条目（弹窗编辑）
   - 删除条目（单条/批量）
   - 搜索过滤

4. 数据存储
   - CSV文件：存储为文件对象
   - 手动条目：存储为数组
   - 使用localStorage持久化

数据结构

// 知识库条目
interface KnowledgeBaseItem {
  id: string
  chinese: string
  english: string
  source: 'manual' | 'csv'
  createdAt: string
}

// CSV文件
interface CSVFile {
  id: string
  filename: string
  content: string
  items: Array<{ chinese: string; english: string }>
  createdAt: string
}

// 知识库Store
interface KnowledgeBaseStore {
  csvFiles: CSVFile[]
  manualItems: KnowledgeBaseItem[]
  
  // 方法
  uploadCSV(file: File): Promise<void>
  deleteCSV(fileId: string): void
  exportCSV(fileId: string): void
  addManual(chinese: string, english: string): void
  editManual(id: string, chinese: string, english: string): void
  deleteManual(id: string): void
  batchDeleteManual(ids: string[]): void
  getAllItems(): KnowledgeBaseItem[]  // 合并CSV和手动条目
}

翻译时使用

上传文档时，自动带上知识库（CSV + 手动）：
```typescript
// 合并所有知识库条目
const allItems = [
  ...kb.csvFiles.flatMap(file => file.items),
  ...kb.manualItems.map(item => ({ chinese: item.chinese, english: item.english }))
]

// 传给后端
formData.append('knowledgeBase', JSON.stringify(allItems))
```

后端使用知识库：
- 翻译时优先使用知识库中的术语
- 提取术语标注时参考知识库
- 确保术语翻译一致性


---

🔌 后端对接

开发策略

- 开发期: 使用 Mock API（假数据）
- 后端就绪后: 切换到真实接口
- 切换: 只需改配置
Mock API 设计

// api/mock.ts
export const mockTranslate = (request) => {
  return Promise.resolve({
    translatedText: `[Mock] ${request.text}`
  })
}

需要确认的接口

文档翻译接口: POST /api/document/upload
- 请求: files (File[]), sourceLang (string), targetLang (string), knowledgeBase (string, 可选)
- 响应: { success: boolean, results: Array<{ fileId, filename, fileType, originalText, translatedText, termAnnotations }> }
- 功能: 上传文档 → 解析 → 翻译 → 提取术语 → 返回结果

接口参数说明：
- files: 文件数组（PDF/docx/markdown）
- sourceLang: 源语言（如"中文"）
- targetLang: 目标语言（如"英文"）
- knowledgeBase: 知识库JSON（可选，格式: [{chinese, english}]）

响应数据结构：
- fileId: 文件ID
- filename: 文件名
- fileType: 文件类型
- originalText: 原文
- translatedText: 译文
- termAnnotations: 术语标注（包含术语原文、译文、位置索引）

知识库参数使用：
- 前端在upload时自动带上知识库（CSV + 手动条目合并）
- 后端翻译时优先使用知识库中的术语
- 术语提取时参考知识库确保一致性

---

🏗️ 文件架构

项目结构

frontend/
├── src/
│   ├── components/              # 组件
│   │   ├── FileExplorer/        # 文件树组件
│   │   │   ├── FolderTree.vue
│   │   │   ├── FolderNode.vue
│   │   │   └── index.ts
│   │   ├── TranslationView/     # 翻译视图
│   │   │   ├── OriginalPanel.vue    # 原文编辑区
│   │   │   ├── TranslatedPanel.vue  # 译文显示区
│   │   │   ├── TabsArea.vue         # 标签页切换区
│   │   │   └── TranslationEditor.vue
│   │   ├── DocumentUpload/       # 上传组件
│   │   │   ├── UploadDialog.vue   # 上传对话框
│   │   │   └── DragDropUpload.vue
│   │   ├── ProgressToast/       # 进度通知
│   │   │   └── UploadProgress.vue
│   │   ├── SettingPanel/        # 设置面板
│   │   │   └── ThemeSelector.vue
│   │   └── KnowledgeBase/       # 知识库组件
│   │       ├── CSVSection.vue   # CSV文件管理区
│   │       ├── ManualSection.vue # 手动术语管理区
│   │       ├── ItemDialog.vue   # 添加/编辑弹窗
│   │       └── KnowledgePanel.vue # 知识库面板
│   ├── stores/                   # Pinia状态管理
│   │   ├── folderStore.ts       # 文件夹管理
│   │   ├── translationStore.ts  # 翻译记录
│   │   ├── settingStore.ts      # 设置
│   │   ├── editorStore.ts       # 编辑器状态
│   │   └── knowledgeBaseStore.ts # 知识库管理
│   ├── utils/                   # 工具函数
│   │   ├── fileParser.ts       # 文档解析
│   │   ├── storage.ts          # localStorage封装
│   │   └── db.ts               # 数据操作
│   ├── api/                     # API接口
│   │   ├── client.ts           # API客户端
│   │   ├── mock.ts             # Mock数据
│   │   ├── translate.ts        # 翻译接口
│   │   └── types.ts            # 类型定义
│   ├── styles/                  # 样式
│   │   ├── theme.css           # 主题样式
│   │   └── variables.css       # CSS变量
│   ├── types/                   # TypeScript类型
│   │   ├── storage.interface.ts
│   │   └── translation.types.ts
│   ├── App.vue                  # 根组件
│   └── main.ts                  # 入口文件
├── electron/                    # Electron主进程
│   ├── main.js                  # 主进程入口
│   └── preload.js              # 预加载脚本
├── public/                      # 静态资源
├── package.json
├── tsconfig.json
├── vite.config.ts
└── electron-builder.yml        # 打包配置

核心文件说明

组件目录

components/FileExplorer/
- FolderTree.vue: 文件树主组件
- FolderNode.vue: 文件夹节点组件
- 功能: 文件树渲染、拖拽移动、右键菜单
components/TranslationView/
- OriginalPanel.vue: 原文编辑区
- TranslatedPanel.vue: 译文显示区
- TabsArea.vue: 标签页切换区
- 功能: 文档编辑、标签页切换
components/DocumentUpload/
- UploadDialog.vue: 上传对话框
- DragDropUpload.vue: 拖拽上传组件
- 功能: 文件选择、文件夹选择、上传
components/ProgressToast/
- UploadProgress.vue: 进度通知浮窗
- 功能: 显示上传/翻译进度
Stores目录

stores/folderStore.ts
export const useFolderStore = defineStore('folder', () => {
  const folders = ref<Folder[]>([])
  const currentFolderId = ref<string | null>(null)
  
  // 加载文件夹树
  const loadFolders = async () => {}
  
  // 创建文件夹
  const createFolder = async (name: string) => {}
  
  // 删除文件夹
  const deleteFolder = async (id: string) => {}
  
  // 移动文件夹
  const moveFolder = async (id: string, targetId: string) => {}
  
  return { folders, currentFolderId, loadFolders, createFolder, deleteFolder, moveFolder }
})

stores/translationStore.ts
export const useTranslationStore = defineStore('translation', () => {
  const translations = ref<TranslationRecord[]>([])
  const currentTranslation = ref<TranslationRecord | null>(null)
  
  // 翻译文本
  const translateText = async (text: string, sourceLang: string, targetLang: string) => {}
  
  // 保存翻译
  const saveTranslation = async (translation: TranslationRecord) => {}
  
  // 加载翻译
  const loadTranslation = async (id: string) => {}
  
  return { translations, currentTranslation, translateText, saveTranslation, loadTranslation }
})

stores/editorStore.ts
export const useEditorStore = defineStore('editor', () => {
  const openedFiles = ref<TabFile[]>([])  // 标签页堆叠
  const currentFile = ref<string | null>(null)
  
  // 打开文件
  const openFile = async (translationId: string) => {}
  
  // 关闭文件
  const closeFile = (id: string) => {}
  
  // 切换文件
  const switchFile = (id: string) => {}
  
  return { openedFiles, currentFile, openFile, closeFile, switchFile }
})

stores/settingStore.ts
export const useSettingStore = defineStore('setting', () => {
  const colorMode = useDark()
  
  // 设置主题
  const setTheme = (theme: 'light' | 'dark' | 'auto') => {}
  
  return { colorMode, setTheme }
})

API目录

api/client.ts
import axios from 'axios'
import { mockTranslate } from './mock'

const USE_MOCK = true  // 开发期用Mock
const baseURL = 'https://your-backend.com/api'

const apiClient = axios.create({ baseURL })

export const translateApi = {
  async translate(request: TranslationRequest): Promise<TranslationResponse> {
    if (USE_MOCK) {
      return mockTranslate(request)  // Mock
    }
    return apiClient.post('/translate/text', request)  // 真实接口
  }
}

api/mock.ts
export const mockTranslate = (request: TranslationRequest) => {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve({
        translatedText: `[Mock] ${request.text}`,
        sourceLang: request.sourceLang,
        targetLang: request.targetLang
      })
    }, 500)
  })
}

Utils目录

utils/storage.ts
// localStorage 封装
export function saveTranslation(translation: TranslationRecord) {
  const key = 'translations'
  const existing = loadTranslations()
  existing.push(translation)
  localStorage.setItem(key, JSON.stringify(existing))
}

export function loadTranslations(): TranslationRecord[] {
  const data = localStorage.getItem('translations')
  return data ? JSON.parse(data) : []
}


---

📋 开发计划

Week 1: 项目搭建与基础布局

Day 1-2: 项目搭建
# 创建项目
npm create vue@latest translate-app
cd translate-app

# 安装依赖
npm install element-plus @element-plus/icons-vue
npm install pinia @vueuse/core
npm install axios
npm install electron electron-builder --save-dev

Day 3-5: 布局实现
[] 实现三栏布局（左侧文件树 + 中间原文 + 右侧译文）
[] 顶部工具栏（上传按钮、主题切换）
[] 文件树组件（Mock数据）
[] 翻译编辑区（TextArea）

Day 6-7: 基础功能
[] 主题切换（浅色、深色、跟随系统）
[] 文档上传基础功能（Mock）
[] Mock API

Week 2: 核心功能开发

Day 8-10: 文件管理
[] 文件树组件完善
- 创建文件夹
- 重命名
- 删除
- 拖拽移动
[] 文件状态显示
[] 右键菜单

Day 11-14: 翻译功能
[] 上传功能完善
[] 翻译功能实现
[] 并发请求处理
[] 进度显示

Week 3: 完善功能

Day 15-17: 标签页与自动打开
[] 标签页切换组件
[] 文件自动打开功能
[] 堆叠切换逻辑

Day 18-21: 本地缓存与交互优化
[] localStorage封装
[] 翻译历史记录
[] 交互优化
[] 错误处理

Week 4: Electron打包与测试

Day 22-25: Electron集成
[] 配置Electron
[] 打包为.exe
[] 测试应用

Day 26-28: 优化与发布
[] Bug修复
[] 性能优化
[] 最终测试


---

🎯 关键技术点

Element Plus 组件使用

组件
用途
el-tree
文件树
el-upload
文件上传
el-input type="textarea"
原文/译文编辑
el-tabs
标签页切换
el-dialog
上传对话框
el-notification
进度通知

核心技术

17. 并发处理: Promise.all() 同时发送多个翻译请求
18. 进度显示: 收到结果就+1，显示"已完成/总数"
19. 标签页切换: 堆叠显示最近打开的N个文件
20. 自动打开: 翻译完成后自动切换到最新文件
21. 本地缓存: localStorage + JSON

---

✅ 总结

核心特性

22. ✅ 参考Obsidian和VS Code的设计
23. ✅ 左侧文件树 + 中间原文 + 右侧译文
24. ✅ 顶部上传入口 + 每个文件夹上传入口
25. ✅ 上传对话框底部选择目标文件夹
26. ✅ 右下角统一进度浮窗
27. ✅ 标签页切换区在编辑区上方
28. ✅ 翻译完成后自动打开（堆叠切换）
29. ✅ 不限制并发数，后端高并发
30. ✅ 显示格式：已完成/总数
技术栈

- Vue3 + TypeScript
- Element Plus
- Pinia
- Electron
- localStorage + JSON
开发策略

- 开发期使用Mock API
- 后端就绪后切换真实接口
- 一个月交付周期

---

📞 待确认事项

[] 后端接口规范
[] 文件大小限制
[] 支持的文件格式
[] 错误处理机制
